{
  "name": "installer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/installer/src",
  "projectType": "library",
  "tags": ["type:context", "scope:installation"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-lib"],
      "outputs": ["{workspaceRoot}/dist/packages/installer"],
      "cache": true,
      "inputs": [
        "production",
        "^production",
        "{workspaceRoot}/tools/build-cli.mjs"
      ],
      "options": {
        "command": "node tools/build-cli.mjs installer"
      }
    },
    "build-lib": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/installer",
        "main": "packages/installer/src/index.ts",
        "tsConfig": "packages/installer/tsconfig.lib.json",
        "assets": ["packages/installer/*.md"]
      }
    }
  }
}
