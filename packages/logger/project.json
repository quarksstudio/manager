{
  "name": "logger",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/logger/src",
  "projectType": "library",
  "tags": ["type:adapter", "scope:shared"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/logger",
        "main": "packages/logger/src/index.ts",
        "tsConfig": "packages/logger/tsconfig.lib.json",
        "assets": ["packages/logger/*.md"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/logger/jest.config.cts"
      }
    },
    "build-lib": {
      "executor": "nx:noop",
      "dependsOn": ["build"],
      "outputs": ["{workspaceRoot}/dist/packages/logger"]
    }
  }
}
