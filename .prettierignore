# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
# pnpm maintains its own lockfile format; prettier would rewrite it on every install
/pnpm-lock.yaml
# long-running conversation/release log, not source documentation
/request.md
# Electron staging output
**/.desktop-app
